# Database Configuration
# When running with docker-compose, this is set automatically
# For local development, uncomment and use:
# DATABASE_URL=postgresql://module_guide:module_guide_dev_password@localhost:5432/module_guide

# These are configured automatically by docker-compose
# but you can override them if needed
# FRONTEND_ADDRESS=localhost
# FRONTEND_PORT=5173
# BACKEND_ADDRESS=localhost
# BACKEND_PORT=5000
